# Microsoft Authenticator Demo Environment for Kubernetes
# This file is used for local Kubernetes deployment with Kind

# Database Configuration
POSTGRES_USER=martyuser
POSTGRES_PASSWORD=martypassword
POSTGRES_DB=martydb
POSTGRES_PORT=5432

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Service Ports (NodePort exposure)
ISSUER_PORT=30000
VERIFIER_PORT=30001

# Kubernetes Local Access Configuration
PORT_FORWARD_MODE=false
ISSUER_BASE_URL=http://localhost:30000
VERIFIER_BASE_URL=http://localhost:30001
ISSUER_API_URL=http://localhost:30000
VERIFIER_API_URL=http://localhost:30001
CREDENTIAL_ISSUER_DID=did:web:localhost%3A30000
VERIFIER_DID=did:web:localhost%3A30001
CORS_ORIGINS=http://localhost:30000,http://localhost:30001

# Vault Configuration
VAULT_ROOT_TOKEN=myroot
VAULT_PORT=8200

# Service Internal URLs (for inter-service communication)
ISSUER_INTERNAL_URL=http://issuer-api-service:8000
VERIFIER_INTERNAL_URL=http://verifier-api-service:8001

# Logging
LOG_LEVEL=INFO

# Kubernetes Deployment
DEPLOYMENT_MODE=kubernetes
NAMESPACE=marty-microsoft-demo
CLUSTER_NAME=marty-microsoft-demo

# Instructions:
# 1. Create Kind cluster: make k8s-cluster-create
# 2. Deploy services: make k8s-deploy
# 3. Test endpoints: make test-endpoints (will use these URLs)
# 4. Access issuer at: http://localhost:30000
# 5. Access verifier at: http://localhost:30001
# 6. View documentation at: http://localhost:30000/docs and http://localhost:30001/docs