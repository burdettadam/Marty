# Additional dependencies for Cedar Policy Engine integration
watchdog>=3.0.0  # For hot-reload file watching
PyYAML>=6.0      # For YAML rule pack parsing

# Note: Cedar policy Python bindings
# The actual Cedar policy library for Python would be added here when available
# For now, we're using a compatible interface that can be replaced with the real library

# Development dependencies for Cedar integration
jsonschema>=4.0.0  # For rule pack schema validation